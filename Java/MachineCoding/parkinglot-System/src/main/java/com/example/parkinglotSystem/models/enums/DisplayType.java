package com.example.parkinglotSystem.models.enums;

public enum DisplayType {
    FREE_SLOTS,         // Displays all free slots
    OCCUPIED_SLOTS,     // Displays all occupied slots
    FREE_COUNT,         // Displays the count of free slots
    OCCUPIED_COUNT      // Displays the count of occupied slots
}
